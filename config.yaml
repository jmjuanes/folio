## default configuration for folio-studio
## https://github.com/jmjuanes/folio/blob/main/config.yaml

## port where folio server will listen
## this value can be overrided using the FOLIO_PORT env variable
port: 8080

## configure the authentication method
## allowed values: "access_token"
authentication: 'access_token'

## configure here your access token, if want a fixed access token
## if not provided, a new one will be generated and printed in logs every time the server is restarted
## you can override this token also using the FOLIO_ACCESS_TOKEN env variable
# access_token: "123456abcd"

## information about the user to display in the website
## only applicable if the authentication method is 'access_token'
user_name: 'john_doe'
user_display_name: 'John Doe'
# user_avatar_url: 'https://....'

## configure the storage method to use
## allowed values: 'local', 'memory'
storage: 'local'

## configure the local storage
## only applicable if the storage method is 'local'
storage_file: 'data/folio.db'

## enable or disable cross origin requests
# cors: false
# cors_origin: "*"
# cors_allowed_methods: "GET,POST"

## configure the generation of JWT tokens for authentication
## secret: allows to specify a fixed secret key to generate jwt tokens.
# jwt_token_secret: "abcd"
## expiration: specify the expiration time. Times are parsed by https://www.npmjs.com/package/ms
# jwt_token_expiration: "1y"

## ai configuration
## enable or disable the ai service
# ai_enabled: false
## port where the ai service will listen. Can be overrided using the FOLIO_AI_PORT env variable
# ai_port: 8081
## base url to the AI provider (openai or groq)
# ai_base_url: 'https://api.openai.com/v1'
## api key to the AI provider
# ai_apikey: 'your_api_key'
## model to use
# ai_model: 'gpt-3.5-turbo'

## configure the folder where the frontend app is located
app_directory: 'app/'

## branding configuration (title, favicon, etc.)
## app_config:
##  title: 'folio.'
