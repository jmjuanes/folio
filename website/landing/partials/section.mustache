<section class="w-full py-32 sm:py-40 md:py-48 {{this.className}}" style="{{this.style}}" data-role="section">
    <div class="mx-auto max-w-6xl px-6">
        <div class="flex flex-col gap-8 items-center" data-role="section:header">
            {{#if this.icon}}
            <div class="flex mb-0">
                <div class="flex items-center opacity-90">
                    {{=icon icon=this.icon className="w-28 h-28"}}
                </div>
            </div>
            {{/if}}
            <div class="max-w-4xl w-full mx-auto flex flex-col items-center">
                {{#if this.label}}
                <div class="mb-2 font-bold text-xl md:text-2xl text-center opacity-70">
                    {{!this.label}}
                </div>
                {{/if}}
                <div class="text-5xl sm:text-6xl md:text-7xl lg:text-8xl leading-none font-extrabold text-center">
                {{#each this.title}}
                    <span class="{{this.className}}">{{!this.text}}</span>
                {{/each}}
                </div>
            </div>
            {{#if this.description}}
            <div class="max-w-2xl text-xl md:text-2xl text-center opacity-80">
                {{!this.description}}
            </div>
            {{/if}}
            {{#if this.actions}}
            <div class="flex flex-col sm:flex-row justify-center items-center gap-6">
                {{#each this.actions}}
                <a href="{{this.url || this.link}}" class="font-bold text-2xl hover:underline">
                    <span>{{!this.text}} →</span>
                </a>
                {{/each}}
            </div>
            {{/if}}
        </div>
        {{#if @content}}
        <div class="mt-24">
            {{!@content}}
        </div>
        {{/if}}
    </div>
</section>
