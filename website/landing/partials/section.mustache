<section class="mx-auto max-w-6xl px-6 py-16 md:py-20" data-role="section">
    <div class="flex flex-col gap-4 mb-12 items-center" data-role="section:header">
        <div class="max-w-xl text-3xl md:text-4xl text-gray-950 leading-tight font-extrabold text-center">
            {{!this.title}}
        </div>
        {{#if this.description}}
        <div class="max-w-lg text-gray-900 text-lg text-center">
            {{!this.description}}
        </div>
        {{/if}}
    </div>
    {{!@content}}
</section>
