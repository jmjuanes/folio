// default port where the server will run
export const DEFAULT_PORT = 8080;

export enum OBJECT_TYPES {
    BOARD = "board",
    USER = "user",
    LIBRARY = "library",
};

export enum LOGIN_API_ENDPOINTS {
    login = "api/login",
};

export enum STATUS_API_ENDPOINTS {
    status = "api/status",
};

export enum GRAPHQL_API_ENDPOINTS {
    graphql = "api/graphql",
};
